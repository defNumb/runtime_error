// app color
import 'package:flutter/material.dart';

// SETTINGS
const boxContainerColor = Colors.white70;
const appbarColor = const Color.fromARGB(255, 26, 134, 223);
const Color primaryColor = Colors.amber;
const fontColor = Colors.white;
const fontType = 'Quicksand';

// a list with two strings dog and cat

List<String> speciesList = ['Dog', 'Cat'];
